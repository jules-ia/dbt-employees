employees:
  target: staging
  outputs:
    staging:
      type: snowflake
      account: "not needed"
      user: "not needed"
      role: DBT__B_ROLE
      database: DBT_STAGING
      schema: PUBLIC
      warehouse: DATAMESH_XS
    production:
      type: snowflake
      account: "not needed"
      user: "not needed"
      role: DBT__B_ROLE
      database: DBT
      schema: PUBLIC
      warehouse: DATAMESH_XS
